<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ddd">

	<!-- update 동적 update dept set dname = ? , loc = ? where deptno = ? -->
	<update id="updateDept" parameterType="Dept">
		update dept
		<set>
			<if test="dname != null">dname=#{dname},</if>
			<if test="loc != null">loc=#{loc}</if>
		</set>
		where deptno = #{deptno}
	</update>

	<!-- foreach -->
	<select id="selectByMultiDeptno" resultType="Dept" 
	       parameterType="arraylist">
		SELECT *
		FROM dept
		WHERE deptno in
		<foreach item="item" index="index" collection="list" open="("
			separator="," close=")">
			#{item}
		</foreach>
	</select>
	
	<insert id="multiInsert" parameterType="arraylist">
	   insert all
	    <foreach collection="list" item="xxx">
	      into dept (deptno,dname, loc )
	      values ( orderAll() ,  #{xxx.dname} , #{xxx.loc})
	    </foreach>
	   select * from dual
	</insert>

    <delete id="multiDelete" parameterType="arraylist">
       delete from dept
       <where>
         deptno IN
         <foreach item="item" index="index" collection="list" open="("
			separator="," close=")">
			#{item.deptno}
		</foreach>
       </where>
    </delete>
</mapper>



