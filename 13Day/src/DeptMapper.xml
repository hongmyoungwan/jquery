<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa">
	<insert id="a" parameterType="Dept">
	  insert into dept ( deptno, dname, loc )
	  values ( #{deptno}, #{dname} , #{loc} )
	</insert>

    <delete id="deleteByDeptno" parameterType="int">
       delete from dept
       where deptno = #{deptno}
    </delete>

  <update id="updateByDeptno" parameterType="Dept">
      update dept
      set dname = #{dname} , loc = #{loc}
      where deptno = #{deptno}
  </update>

 <select id="selectByDeptno" parameterType="int" 
          resultType="Dept">
    select deptno, dname,loc
    from dept
    where deptno = #{deptno}
 </select>

 <select id="selectAll" resultType="Dept">
   select deptno , dname,loc
    from dept
 </select> 
</mapper>










